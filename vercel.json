{
    "builds":[{
        "src": "server.js",
        "use": "@vercel/node",
        "config": {
            "includesFiles": ["db.json"]
        }
    }],
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/",
            "dest": "server.js"
        }
    ]
}