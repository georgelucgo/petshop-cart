{
    "builds":[{
        "src": "server.js",
        "use": "@vercel/node",
        "config": {
            "includesFiles": ["db.json"]
        }
    }],
    "routes": [
        {
            "source": "/(.*)",
            "destination": "/",
            "dest": "server.js"
        }
    ]
}